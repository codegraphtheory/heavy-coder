# Copy to .env in an installed profile if you choose env-based credentials.
# Do not commit real values.
# Heavy Coder defaults to Hermes xAI Grok OAuth, configured with:
# hermes auth add xai-oauth
GITHUB_TOKEN=
