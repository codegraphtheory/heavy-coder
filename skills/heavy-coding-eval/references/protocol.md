# Evaluation Protocol

The preregistration file should name tasks, conditions, runtime limits, model configuration, and analysis method before any result is collected.

Result rows must validate against `schemas/evaluation-result.schema.json`.
