# Issue to unattended merge example

Status: planned and fail-closed.

Prompt:

```text
Take issue #123 through implementation, CI repair, and merge when all policy gates pass.
```

Expected future behavior: Heavy Coder proceeds only if the repository is allowlisted, the trigger is authorized, checks pass, head SHA matches, and isolation requirements are satisfied.
